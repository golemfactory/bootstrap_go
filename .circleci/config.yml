version: 2
jobs:
  build:
    docker:
      - image: igorgolem/bootstrap_go

    working_directory: /go/src/github.com/golemfactory/bootstrap_go

    steps:
      - checkout

      - run: go get -v -t ./...
      - run: go test -v -race ./...
      - run: go vet -v ./...
